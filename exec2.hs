import Prelude hiding (sum)

sum :: [Int] -> Int --recebe uma lista de inteiros e retorna um inteiro, se a lista contém apenas um elemento [x], a soma é igual ao próprio elemento.
sum [x] = x

-- vai retorar o unico elemento presente na lista 
